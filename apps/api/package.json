{
  "name": "@orbit/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 migrations apply orbit-db",
    "db:migrate:local": "wrangler d1 migrations apply orbit-db --local"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.3",
    "@hono/zod-validator": "^0.4.3",
    "@orbit/shared": "*",
    "hono": "^4.7.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.20",
    "@cloudflare/workers-types": "^4.20250307.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.8",
    "wrangler": "^4.24.3"
  }
}
